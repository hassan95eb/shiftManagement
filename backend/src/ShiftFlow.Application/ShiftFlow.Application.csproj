<Project Sdk="Microsoft.NET.Sdk">

  <!-- Common build settings: see backend/Directory.Build.props and
       backend/src/Directory.Build.props. -->

  <ItemGroup>
    <!-- EF Core abstractions only. IAppDbContext exposes DbSet<T> and
         SaveChangesAsync; the concrete DbContext lives in Infrastructure.
         Microsoft.EntityFrameworkCore also brings IServiceCollection in
         transitively for DependencyInjection.cs. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore" Version="10.0.11" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ShiftFlow.Domain\ShiftFlow.Domain.csproj" />
  </ItemGroup>

</Project>
